# Showcase: Testing JPA Database Repositories
Showcase demonstrating how JPA database repositories can be testing with the
help of JUnit 5, an in memory H2 database and Spring Boot's `@DataJpaTest` support.
